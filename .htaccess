# Draft redirect file for Apache. Final rules should be confirmed with the current server files.
DirectoryIndex index.html index.php

RewriteEngine On

# Route legacy case-list URLs to the new market platform.
Redirect 301 /cases/ /market/

# Preserve existing root-managed services if they exist on the server.
RewriteRule ^ai-satei/ - [L]

# Example legacy redirects. Replace the left-hand paths after current files arrive.
# Redirect 301 /service/ /business/
# Redirect 301 /strength/ /about/
# Redirect 301 /company.html /company/
